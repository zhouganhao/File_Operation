#include <stdio.h>
#include "libconfig.h"

int main(void)
{
	config_ini();
	char name[30] = {0};
	if(Read_Config_Value("./config.ini", "NAME", name) < 0)
	{
		printf("read config error\n");
	}else
	{
		printf("success  value:%s\n",name);
	}
	//printf("hello world!\n");
	return 0;
}
